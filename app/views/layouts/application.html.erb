<!DOCTYPE html>
<html>
  <head>
    <title>DockwaRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://fonts.googleapis.com/css?family=Rock+Salt" rel="stylesheet" type="text/css" />
  </head>

  <body>
    <div id='container'>
      <header>
        Outdoor.sy
      </header>

      <div class='wrapper'>
        <% flash.each do |type, msg| %>
          <%= tag.div class: "flash flash-#{type}" do  %>
            <%= msg %>
          <% end %>
        <% end %>
      </div>


      <div class='wrapper'>
        <%= yield %>
      </div>

      <footer>
        &copy; 2024 Andrew Canfield
      </footer>
    </div>

  </body>
</html>
